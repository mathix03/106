DROP TABLE IF EXISTS t_ligne_commande;

CREATE TABLE t_ligne_commande (
    ligne_commande_id INTEGER PRIMARY KEY,
    commande_fk INTEGER NOT NULL,
    produit_fk INTEGER NOT NULL,
    quantite INTEGER NOT NULL DEFAULT 1,
    prix_unitaire_facture DECIMAL(10,2) NOT NULL,
    parent_ligne_fk INTEGER NULL
);